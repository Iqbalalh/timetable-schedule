"use client";
import React from "react";
import { Button, Form, Input, Space } from "antd";

const Routing = () => {
  const [form] = Form.useForm();
  return (
    <main className="flex bg-blue-200 min-h-screen">
      <Button>Contoh Aja</Button>
    </main>
  );
};

export default Routing;
